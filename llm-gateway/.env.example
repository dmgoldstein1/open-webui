# AWS local emulation configuration (LocalStack + DynamoDB Local)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Local endpoints
DYNAMODB_ENDPOINT_URL=http://localhost:8000
SQS_ENDPOINT_URL=http://localhost:4566

# DynamoDB table
DDB_TABLE=Conversations

# SQS queues (fill from OpenTofu outputs)
# SQS_QUEUE_URL=
# SQS_PRIORITY_QUEUE_URL=

# Local LLM endpoints
# For both workers we use OpenAI-compatible chat API for simplicity
VLLM_BASE_URL=http://localhost:8000
VLLM_MODEL=llama-3-8b-instruct

OLLAMA_OPENAI_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llava:13b

# Server
PORT=8787
ALLOW_ORIGIN=*
